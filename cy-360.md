请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：复古胶片相机租赁平台 —— 面向摄影爱好者，提供胶片相机租赁、胶片销售和冲洗服务的摄影文化平台。

**项目类型**：全栈Web应用

**功能需求**：
1. **相机 catalog（品牌/型号/成色）**：录入胶片相机信息（品牌如佳能/尼康/徕卡/禄来、型号、生产年份、成色等级、日租金），上传实物照片和样张。
2. **胶片库存管理（型号/ISO/张数）**：管理各型号胶卷库存（如柯达Portra 400/富士C200/伊尔福HP5），记录ISO感光度、剩余张数和有效期，与相机租赁联动推荐。
3. **租赁时长与押金**：用户选择相机和租赁天数（支持1天起租），系统自动计算租金和押金，归还时验收相机状态，无损退还押金，损坏按赔偿标准扣款。
4. **冲洗服务预约**：用户拍摄完成后可预约胶片冲洗服务（C-41彩色负片/E-6反转片/BW黑白），选择冲扫精度，系统记录冲扫进度并推送取片通知。
5. **作品展示墙与点赞**：用户冲扫完成后上传作品到展示墙，其他用户可浏览、点赞和收藏，优秀作品置顶推荐，营造胶片摄影社区氛围。

**技术要求**：
- 英文短名（用于 Docker Compose）：`lpfilmcamera`
- 前端端口：`28520`，后端端口：`29520`
- 前端框架：Vue 3 + TypeScript
- UI 库：Element Plus
- 构建工具：Vite
- 后端框架：Spring Boot + Java
- 数据库：PostgreSQL
- 认证方式：JWT
- 其他依赖：JPA（Hibernate）、Maven

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=lpfilmcamera`，确保 Compose 项目名不依赖目录名（中文目录名会被规范化为空导致 `project name must not be empty` 错误）
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃，会产生 `the attribute version is obsolete` 警告）
- 顶层声明 `name: lpfilmcamera` 作为兜底，避免目录名为中文时项目名为空
- 所有服务使用 `container_name` 命名容器，便于管理；容器名建议带 `${COMPOSE_PROJECT_NAME:-lpfilmcamera}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:29520`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也建议配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `80:80`，后端按框架默认端口，数据库使用标准端口
- 所有可变配置（端口、数据库账号密码、JWT 密钥等）通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务，不依赖额外的 `-p` 参数

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME`：英文短名（小写字母/数字/`-`/`_`），如 `lpfilmcamera`
- 数据库连接相关：`DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- 后端：`JWT_SECRET` 或等价的密钥配置
- 端口映射：`FRONTEND_PORT`、`BACKEND_PORT`、`DB_PORT`（可选，便于本地冲突时调整）

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:29520/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由
- WebSocket 场景需配置 `Upgrade` / `Connection` 头

自检清单（生成代码后必须确认）：
1. 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示全部容器 healthy
3. 浏览器访问前端端口能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`（不需要 `-p`）

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: lpfilmcamera   # 顶层 name 字段兜底，避免中文目录名导致 project name 为空

services:
  db:
    image: postgres:15
    container_name: ${COMPOSE_PROJECT_NAME:-lpfilmcamera}-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-lpfilmcamera}-backend
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-lpfilmcamera}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend

volumes:
  db_data:
```

`.env.example` 参考：

```dotenv
# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=lpfilmcamera

DB_NAME=app
DB_USER=app
DB_PASSWORD=app_pwd
DB_ROOT_PASSWORD=root_pwd

JWT_SECRET=change_me_to_a_long_random_string

FRONTEND_PORT=80
```

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`gitignore` 内容按项目技术栈适配（至少包含 node_modules/、dist/、.env、.DS_Store、*.log、.idea/、.vscode/、backend/dist/、frontend/dist/）。
